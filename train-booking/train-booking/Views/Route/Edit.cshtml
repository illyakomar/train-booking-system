@model train_booking.ViewModels.Routes.RoutesViewModel

@{
    ViewData["Title"] = "Редагування маршруту";

    ViewData["Action"] = "Edit";
}

<div class="row">
    <div class="col-xl-6 col-lg-8 col-md-10 col-sm-12 col-xs-12 stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Редагувати маршрут</h4>
                @await Html.PartialAsync("_RouteForm", Model, ViewData)
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}