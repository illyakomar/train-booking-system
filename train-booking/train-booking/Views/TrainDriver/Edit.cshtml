@model train_booking.ViewModels.TrainDrivers.TrainDriverFormViewModel
@{
    ViewData["Title"] = "Редагувати машиніста";

    ViewData["Action"] = "Edit";
}

<div class="row">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Форма машиніста</h4>
                @await Html.PartialAsync("_TrainDriveForm", Model, ViewData)
            </div>
        </div>
    </div>
</div>
